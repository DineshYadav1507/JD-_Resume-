'use client';import{useEffect,useState}from"react";import{useRouter}from"next/navigation";export default function Admin(){const[u,setU]=useState<any>(null),[users,setUsers]=useState<any[]>([]),[msg,setMsg]=useState(""),r=useRouter();async function load(){const me=await fetch("/api/me");if(!me.ok)return r.push("/login");const m=await me.json();if(m.role!=="ADMIN")return r.push("/dashboard");setU(m);const x=await fetch("/api/admin/users");if(x.ok)setUsers(await x.json())}useEffect(()=>{load()},[]);async function add(id:string){const x=await fetch("/api/admin/credits",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId:id,amount:5})});setMsg(x.ok?"5 credits added":"Failed");load()}if(!u)return <main className="shell"><div className="card">Loading...</div></main>;return <main className="shell"><nav className="nav"><h1>Admin Panel</h1><a className="btn alt" href="/dashboard">Dashboard</a></nav><div className="card"><p className="muted">{msg}</p>{users.map(x=><div key={x.id} style={{display:"flex",justifyContent:"space-between",padding:"14px 0",borderBottom:"1px solid #eee"}}><div><b>{x.name}</b><div className="muted">{x.email} · {x.role}</div></div><div>{x.credits} credits <button className="btn" onClick={()=>add(x.id)} style={{marginLeft:10}}>+5</button></div></div>)}</div></main>