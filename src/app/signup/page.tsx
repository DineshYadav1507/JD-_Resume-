'use client';import{useState}from"react";import{useRouter}from"next/navigation";import Link from"next/link";export default function Signup(){const[f,setF]=useState({name:"",email:"",password:""}),[err,setErr]=useState(""),r=useRouter();async function go(x:any){x.preventDefault();const z=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});if(z.ok)r.push("/dashboard");else setErr((await z.json()).error||"Signup failed")}return <main className="shell"><div className="card" style={{maxWidth:480,margin:"60px auto"}}><h1>Create account</h1><form onSubmit={go}>{Object.entries(f).map(([k,v])=><div className="field" key={k}><label>{k}</label><input type={k==="password"?"password":k==="email"?"email":"text"} value={v} onChange={x=>setF({...f,[k]:x.target.value})} required/></div>)}{err&&<p className="error">{err}</p>}<button className="btn">Create account</button></form><p>Already have an account? <Link href="/login">Sign in</Link></p></div></main>}