import Link from"next/link";import{currentUser}from"@/lib/auth";export default async function Home(){const u=await currentUser();return <main className="shell"><nav className="nav"><div className="brand">JD Resume AI</div><div>{u?<Link className="btn" href="/dashboard">Dashboard</Link>:<Link className="btn" href="/login">Sign in</Link>}</div></nav><section className="card"><h1>Turn a job description into a ready-to-apply resume.</h1><p className="muted">Maintain one truthful master profile. Paste any US job description. The engine analyzes requirements, researches current job-market context, tailors your resume and writes a hiring-manager cover letter, then exports both as DOCX.</p><div><span className="pill">ATS focused</span><span className="pill">Evidence based</span><span className="pill">DOCX export</span><span className="pill">Multi-user</span><span className="pill">Credits + admin</span></div></section></main>}