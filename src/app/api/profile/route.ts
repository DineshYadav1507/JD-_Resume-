import{NextResponse}from"next/server";import{currentUser}from"@/lib/auth";import{db}from"@/lib/db";import{z}from"zod";const S=z.object({headline:z.string().optional(),phone:z.string().optional(),location:z.string().optional(),linkedin:z.string().optional(),website:z.string().optional(),summary:z.string().optional(),skills:z.any(),certifications:z.any(),experiences:z.any(),projects:z.any(),education:z.any()});export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(u.profile||{});}export async function PUT(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const b=S.parse(await req.json());return NextResponse.json(await db.resumeProfile.upsert({where:{userId:u.id},create:{userId:u.id,...b},update:b}));}