import{NextResponse}from"next/server";import bcrypt from"bcryptjs";import{z}from"zod";import{db}from"@/lib/db";import{setSession}from"@/lib/auth";export async function POST(req:Request){try{const b=z.object({name:z.string().min(2),email:z.string().email(),password:z.string().min(8)}).parse(await req.json());const e=b.email.toLowerCase();if(await db.user.findUnique({where:{email:e}}))return NextResponse.json({error:"Email already registered"},{status:409});const u=await db.user.create({data:{name:b.name,email:e,passwordHash:await bcrypt.hash(b.password,12),profile:{create:{skills:[],certifications:[],experiences:[],projects:[],education:[]}}}});await setSession(u.id);return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message||"Invalid request"},{status:400})}}