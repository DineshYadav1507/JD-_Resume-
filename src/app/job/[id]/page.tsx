import{currentUser}from"@/lib/auth";import{db}from"@/lib/db";import{notFound,redirect}from"next/navigation";export default async function JobResult({params}:{params:Promise<{id:string}>}){const u=await currentUser();if(!u)redirect("/login");const{id}=await params;const j=await db.job.findFirst({where:{id,userId:u.id,status:"COMPLETED"}});if(!j)notFound();const a:any=j.analysis;return <main className="shell"><nav className="nav"><div className="brand">JD Resume AI</div><a className="btn alt" href="/dashboard">Dashboard</a></nav><div className="grid"><section className="card"><h1>{j.title||"Tailored application"}</h1><p className="muted">{j.company||"Company"} · {j.location||"US"}</p><h3>ATS keywords</h3><div>{(a?.atsSkills||[]).map((x:string)=><span className="pill" key={x}>{x}</span>)}</div><h3>Potential gaps</h3><ul>{(a?.missingSkills||[]).map((x:string)=><li key={x}>{x}</li>)}</ul></section><section className="card"><h2>Download</h2><p>Review the generated content before submitting it to an employer.</p><p><a className="btn" href={"/api/jobs/"+j.id+"/download?type=resume"}>Download Resume DOCX</a></p><p><a className="btn alt" href={"/api/jobs/"+j.id+"/download?type=cover"}>Download Cover Letter DOCX</a></p></section></div></main>