'use client';import{useState}from"react";import{useRouter}from"next/navigation";import Link from"next/link";export default function Login(){const[e,setE]=useState(""),[p,setP]=useState(""),[err,setErr]=useState(""),r=useRouter();async function go(x:any){x.preventDefault();const z=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:e,password:p})});if(z.ok)r.push("/dashboard");else setErr((await z.json()).error||"Login failed")}return <main className="shell"><div className="card" style={{maxWidth:480,margin:"60px auto"}}><h1>Sign in</h1><form onSubmit={go}><div className="field"><label>Email</label><input value={e} onChange={x=>setE(x.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={p} onChange={x=>setP(x.target.value)} type="password" required/></div>{err&&<p className="error">{err}</p>}<button className="btn">Sign in</button></form><p>New user? <Link href="/signup">Create account</Link></p></div></main>}